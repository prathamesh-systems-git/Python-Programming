#Python Program to Calculate BMI:
height = 1.65
weight = 84
bmi = weight / height ** 2
print(bmi)

#Converts the Float into Integer by removing everything after decimal
print(int(bmi))

#Rounding a number up to 2 decimal places
print(round(bmi,2))
